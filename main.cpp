#include <iostream>
#include "BST.h"



int main() {


    /*BST* root = nullptr;
    root = root->insert(root, 15);
    root->insert(root, 10);
    root->insert(root, 20);
    root->insert(root, 25);
    std::cout << root->search(root, 15);*/
    return 0;
}





/*
int f(int n)
{
    if (n == 1)
        return n;
    else
        return f(n-1) * n;
}

int f2(int n){
    if (n == 1 )
        return n;
    if (n == 0)
        return n;
    else
        return f2(n-1) + f2(n-2);
}*/
